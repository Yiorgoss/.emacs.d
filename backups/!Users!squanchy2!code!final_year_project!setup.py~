from setuptools, import setup, find_packages

setup(
    name="ml_forecast"
    author="Yiorgos Joseph Makridakis"
    version=0.0.1

    install_requires['numpy', 'click']
    entry_points={
        'console_scripts' : ['ml_forcast = ml_forecast.cli:start']
        }
    )
